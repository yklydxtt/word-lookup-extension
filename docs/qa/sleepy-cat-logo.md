# 蜷睡猫标识替换验证

日期：2026-09-13

状态：用户最后选择第三款「蜷睡猫」，已接入源码并生成更新包；本轮完成本地验证，浏览器重新加载及菜单实际显示尚未验证。

## 变更范围

- 扩展管理页与工具栏使用同一组绿色小猫 PNG：16、32、48、128 像素。
- 菜单原“词”字改为小猫遮罩，继承原有浅深主题色，保留全部设置控件与文案。
- 图形源为 `src/assets/cat-logo.svg`，采用预览第三款的闭眼、蜷卧与卷尾曲线。
- 仅增加构建期依赖 `@resvg/resvg-js@2.6.2`；运行时代码不引入该库。
- 扩展名称、版本 0.1.0、权限、查词卡片、语音、设置逻辑与词库保持不变。

## 验证结果

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 新图标与菜单定向测试 | 通过 | 3 个测试文件、13 项测试通过；图标声明和菜单替换先验证失败，再实施通过 |
| 全部测试 | 通过 | `npm test`：12 个测试文件、54 项测试通过 |
| 类型检查 | 通过 | `npm run typecheck` 退出码 0 |
| 构建与产物检查 | 通过 | `npm run build`、`npm run check:package` 成功；270 个文件、49,730,929 字节、约 47.43 MiB |
| 图标尺寸和透明背景 | 通过 | PNG 签名、IHDR 宽高、RGBA 类型、重复构建结果、矢量源透明像素均检查通过 |
| 小尺寸处理 | 通过 | 16 像素版本轮廓线宽由 6.5 调为 8，眼睛由 5 调为 6.5；其余尺寸保持原图笔画 |
| 生成图形目检 | 通过 | 已查看构建生成的 128 像素 PNG，保留第三款闭眼、猫耳及卷尾 |
| 功能脚本及词库未变 | 通过 | 内容脚本、后台、菜单脚本和词库索引的 SHA-256 与本轮修改前完全相同 |
| 实际浏览器图标与菜单显示 | 未测试 | 原小猫预览空间 4 已不存在，未新建空间绕过；未操作已安装扩展的重新加载按钮 |

功能产物 SHA-256：

| 文件 | 修改前后相同的摘要 |
| --- | --- |
| `dist/content.js` | `501bc65361defd6053ad53f916ce7c0b5523cb4df1e3df728b5a7ea3060e83dd` |
| `dist/background.js` | `b9e62c52f439c1c1f420bc0023d57bba4c2a05485a1ece3f1399d5e3bf153c8e` |
| `dist/popup.js` | `ba005bd9b1c19b7ce98830a48e617808a9b6d1bd7a614fd31afab1bee431fdc2` |
| `dist/dictionary/index.json` | `a0c85a1d2d2660b00744b23046b10a690b11853a07b7a236e6ede9b26259c205` |

已按需求范围、资源引用、构建与运行时隔离、测试覆盖进行本地自审，未派发子代理。首版查词功能的历史实机结果仍见 [原验收记录](ego-lite-acceptance.md)，不将其视为本次图标重载已验证。

## 使更新生效

1. 在 `ego lite` 打开 `chrome://extensions/`。
2. 找到“离线划词查词”，点击“重新加载”。
3. 重新打开插件菜单查看小猫标识，必要时刷新使用查词的网页。

无需重新安装、卸载或改变权限。扩展目录仍为 `/Users/bytedance/Documents/code/test/word-lookup-extension/dist`。
